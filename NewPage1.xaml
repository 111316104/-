<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="PD_app.NewPage1"
             Title="註冊頁面"
             BackgroundColor="#ccd5ae">
    <VerticalStackLayout
        Padding="40"
        Spacing="30"
        HorizontalOptions="Center">

        <Image 
            Source="login.png"
            WidthRequest="150"
            HeightRequest="150"
            HorizontalOptions="Center" />

        <Label 
            Text="註冊"
            FontSize="32"
            TextColor="Black"
            HorizontalOptions="Center" />

        <!-- 帳號欄位 -->
        <Label Text="👤 帳號" FontSize="20" TextColor="Black" HorizontalOptions="Center" />
        <Entry 
            x:Name="usernameEntry"
            Placeholder="請輸入帳號"
            FontSize="20"
            WidthRequest="280"
            HeightRequest="55"
            HorizontalOptions="Center" />

        <!-- 密碼欄位 -->
        <Label Text="🔒 密碼" FontSize="20" TextColor="Black" HorizontalOptions="Center" />
        <Entry 
            x:Name="passwordEntry"
            Placeholder="請輸入密碼"
            IsPassword="True"
            FontSize="20"
            WidthRequest="280"
            HeightRequest="55"
            HorizontalOptions="Center" />

        <!-- 返回登入 -->
        <Button 
            Text="返回登入"
            FontSize="20"
            WidthRequest="280"
            HeightRequest="60"
            BackgroundColor="White"
            TextColor="Black"
            BorderColor="Black"
            BorderWidth="1"
            CornerRadius="10"
            HorizontalOptions="Center"
            Clicked="OnLoginClicked" />

        <!-- 註冊按鈕 -->
        <Button 
            Text="註冊"
            FontSize="20"
            WidthRequest="280"
            HeightRequest="60"
            BackgroundColor="#A18276"
            TextColor="White"
            CornerRadius="10"
            HorizontalOptions="Center"
            Clicked="OnRegisterClicked" />

    </VerticalStackLayout>
</ContentPage>